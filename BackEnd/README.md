# BackEnd (Laravel API)

Estructura base para el backend del proyecto.

## Carpetas

- `app/` logica de negocio y controladores.
- `routes/` rutas API.
- `database/` migraciones, seeders y factories.
- `tests/` pruebas backend.
- `docs/` notas tecnicas de API.
